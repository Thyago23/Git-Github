(git init) // Inicializa ou Reinicia a utilizacao do projeto

(git status) // Informa o estatus do versionamento do projeto

(git add "exemplo.txt") // Adiciona o arquivo em pendencia ao versionamento
(git add .) // Adiciona todos os arquivos em pendencia ao versionamento

(git commit -m "explicacao da alteracao") // Commit com uma mensagem explicando o que foi alterado no projeto

(git config --global user.email "email@gmail.com") // email do github
(git config --global user.name "Nome Do Usuário") // nome no github

(git add origin https...) // setando onde vai ser armazenado no github
(git push --set-upstream origin master) // definindo a branch pro armazenamento

(git pull) // baixando as ultimas atualizacoes do projeto no github

(git clone <link>) // baixa todo um projeto do github

(git push) // enviando tudo que recebeu commit pro github

(git rm meu_arquivo.txt) // Remover arquivo

(git log) // Historico